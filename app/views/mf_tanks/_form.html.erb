<%= form_with(model: @mf_tank, local: true, class: "needs-validation", novalidate: true) do |form| %>
    <% if @mf_tank.errors.any? %>
        <div class="alert alert-danger">
            <h4 class="alert-heading"><%= pluralize(@mf_tank.errors.count, "error") %> prohibited this MF tank from
                being saved:</h4>
            <ul>
                <% @mf_tank.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <% tanks = [%w[3 3], %w[4 4]]
       shifts = [%w[A A], %w[B B], %w[C C]] %>

    <div class="container">

        <!-- General Information Card -->
        <div class="card mb-3">
            <div class="card-header">
                <h5 class="card-title">MF Tank Analysis</h5>
            </div>
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <%= form.label :date, class: "form-label" %>
                        <%= form.text_field :date, class: "form-control" %>
                    </div>
                    <div class="col-md-6">
                        <%= form.label :initial_temp, class: "form-label" %>
                        <%= form.text_field :initial_temp, class: "form-control" %>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <%= form.label :time, class: "form-label" %>
                        <%= form.text_field :time, class: "form-control" %>
                    </div>
                    <div class="col-md-6">
                        <%= form.label :shift, class: "form-label" %>
                        <%= form.select :shift, options_for_select(shifts, @mf_tank.shift) , { prompt: :shift }, class: "form-control" %>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <%= form.label :tank_number, class: "form-label" %>
                        <%= form.select :tank_number, options_for_select(tanks, @mf_tank.tank_number) , { prompt: :tank_number }, class: "form-control" %>
                    </div>
                    <div class="col-md-6">
                        <%= form.label :ph, class: "form-label" %>
                        <%= form.text_field :ph, class: "form-control" %>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <%= form.label :viscosity, class: "form-label" %>
                        <%= form.text_field :viscosity, class: "form-control" %>
                    </div>
                    <div class="col-md-6">
                        <%= form.label :density, class: "form-label" %>
                        <%= form.text_field :density, class: "form-control" %>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <%= form.label 'Cloudy Time', class: "form-label" %>
                        <%= form.text_field :reaction_time, class: "form-control" %>
                    </div>
                    <div class="col-md-6">
                        <%= form.label :solid_content, class: "form-label" %>
                        <%= form.text_field :solid_content, class: "form-control" %>
                    </div>
                </div>
            </div>
        </div>
        <!-- Submit Button -->
        <div class="d-flex mt-3 sticky-bottom pt-2 pb-2">
            <%= form.submit "Save", class: "btn btn-primary flex-fill" %>
        </div>
    </div>
<% end %>

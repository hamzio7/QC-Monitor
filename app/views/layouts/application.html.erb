<!DOCTYPE html>
<html lang="en">

    <head>
        <title>QC Monitor</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>

        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

        <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
        <%= javascript_importmap_tags %>


    </head>

    <body>

        <div class="tools container-fluid mb-3 sticky-top bg-light-subtle border-bottom pt-2 pb-2">
            <div class="container">
                <div class="d-flex justify-content-start align-items-center">
                    <div class="row">
                        <div class="col d-flex align-items-center">
                            <button class="me-2 bg-transparent" style=" border: transparent" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample" aria-controls="offcanvasExample">
                                <span style="font-size: x-large" class="bi bi-list"></span>
                            </button>
                            <%= link_to "QC Monitor", schedules_path, class: "navbar-brand", style: "font-size: large" %>
                        </div>
                    </div>
                </div>

            </div>

            <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
                <div class="offcanvas-header">
                    <% if user_signed_in? %>
                        <%= link_to current_user.username.capitalize, pallets_path, class: "navbar-brand", style: "font-size: large" %>
                    <% end %>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
                <div class="offcanvas-body">
                    <div class="dropdown">
                        <% if user_signed_in? %>
                            <ul class="navbar-nav me-auto">
                                <li class="nav-item">
                                    <%= link_to "Pallets", pallets_path, class: "nav-link" %>
                                </li>
                                <li class="nav-item">
                                    <%= link_to "Overview", schedules_path, class: "nav-link" %>
                                </li>
                                <li class="nav-item">
                                    <%= link_to "UF Tank Analysis", uf_tanks_path, class: "nav-link" %>
                                </li>
                                <li class="nav-item">
                                    <%= link_to "MF Tanks Analysis", mf_tanks_path, class: "nav-link" %>
                                </li>
                                <li class="nav-item">
                                    <%= link_to "Reports", production_days_path, class: "nav-link" %>
                                </li>
                            </ul>
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                    <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
                                </li>
                            </ul>

                        <% else %>
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                    <%= link_to "Login", new_user_session_path, class: "nav-link" %>
                                </li>
                            </ul>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>

        <div>
            <% if notice.present? %>
                <div class="container">
                    <div class=" alert alert-success alert-dismissible fade show  mb-3" role="alert">
                        <div>
                            <%= notice %>
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            <% end %>
            <% if alert.present? %>
                <div class="container">
                    <div class=" alert alert-success alert-dismissible fade show mb-3" role="alert">
                        <div>
                            <%= alert %>
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            <% end %>

            <%= yield %>
        </div>
    </body>
</html>
